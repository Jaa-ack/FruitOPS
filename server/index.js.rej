@@ -135,6 +135,15 @@
   return localDbContainer;
 }
 
+// DEBUG: Ultra-simple endpoint to test if Express routes work at all
+app.get('/api/debug/simple', (req, res) => {
+  console.log('[DEBUG-SIMPLE] Called');
+  res.json({ ok: true, ts: Date.now(), msg: 'Express route works' });
+});
+
+// DEBUG: Test plots with fake data (no DB call)
 app.get('/api/plots', async (req, res) => {
+  console.log('[PLOTS-START] Request received');
   try {
-    const start = Date.now();
+    // TEMP: Return fake data immediately to test if route works
+    console.log('[PLOTS-FAKE] Returning test data');
